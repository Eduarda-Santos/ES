#include <studio.h>

void imprime(int v);

int main (){
    int valor;
    printf("Informe um valor:%i");
    scanf("%i", &valor);
    imprime(valor*2);
}

void imprime(int v){
    if(valor<1000){
        printf("%i", v);
        imprime(v*2);
    }
}
